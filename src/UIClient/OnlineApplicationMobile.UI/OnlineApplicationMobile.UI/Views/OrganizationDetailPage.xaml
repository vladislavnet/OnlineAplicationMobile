<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineApplicationMobile.UI.Views.OrganizationDetailPage"
             Title="Информация об организации">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout Orientation="Horizontal">
                <Label Text="Наименование: "/>
                <Label Text="{Binding Organization.Name}" />
            </StackLayout>
            <Label Text="Описание организации: "/>
            <Label Text="{Binding Organization.Description}" />
            <StackLayout Orientation="Horizontal">
                <Label Text="Email: "/>
                <Label Text="{Binding Organization.Email}" />
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Telephone: "/>
                <Label Text="{Binding Organization.Telephone}" />
            </StackLayout>
            <ListView ItemsSource="{Binding ServiceTypes}" SelectionMode="None" RowHeight="45"/>
            <Button Text="Добавить заявку" Command="{Binding AddApplicationCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
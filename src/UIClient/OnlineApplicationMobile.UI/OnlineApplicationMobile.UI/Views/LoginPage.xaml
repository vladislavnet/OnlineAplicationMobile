<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineApplicationMobile.UI.Views.LoginPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackLayout Orientation="Vertical" Grid.Row="1">
                <Label HorizontalTextAlignment="Center">Авторизация</Label>
                <Entry Placeholder="Email" Keyboard="Email" Text="{Binding Email, Mode=TwoWay}"/>
                <Label Text="{Binding EmailValidateMessage, Mode=OneWay}" IsVisible="{Binding EmailIsVisible, Mode=OneWay}"/>
                <Entry Placeholder="Пароль" IsPassword="True" Text="{Binding Password, Mode=TwoWay}"/>
                <Label Text="{Binding PasswordValidateMessage, Mode=OneWay}" IsVisible="{Binding PasswordIsVisible, Mode=OneWay}"/>
                <Button Text="Войти" Command="{Binding LoginCommand}" CommandParameter="{Binding}"/>
                <Button Text="Регистрация" Command="{Binding RegisterCommand}" CommandParameter="{Binding}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>